// Register module aliases for path resolution at runtime
require('./dist/src/config/paths');

// This file is required at the beginning of the start script
// to ensure that path aliases are resolved correctly at runtime